{
  "name": "metsa-menetleja",
  "private": true,
  "scripts": {
    "dev": "concurrently --names backend,frontend --prefix-colors green,cyan \"npm --prefix backend run start:dev\" \"npm --prefix frontend run dev\"",
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
