{
  "name": "metsa-menetleja",
  "private": true,
  "scripts": {
    "dev": "concurrently --names backend,frontend,ml-api --prefix-colors green,cyan,magenta \"npm --prefix backend run start:dev\" \"npm --prefix frontend run dev\" \"cd tree-detection-model && .venv/bin/uvicorn api:app --host 127.0.0.1 --port 8000\"",
    "install:all": "npm --prefix backend install && npm --prefix frontend install && bash tree-detection-model/install-python.sh"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
